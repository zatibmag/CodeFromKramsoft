<form method="post" action="{{ path('app_sprint_delete', {'id': sprint.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sprint.id) }}">
    <button class="btn">Delete</button>
</form>
